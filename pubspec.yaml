name: main_project
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # مكتبة http لإرسال الطلبات
  http: ^1.2.0

  # مكتبة الأيقونات الافتراضية
  cupertino_icons: ^1.0.8
  provider: ^6.1.5
  date_picker_plus: ^4.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/